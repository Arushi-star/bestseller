<h1> Show Page</h1>

    <div class="row">
	    <div class="col-sm-3 form-group">
	      <b> Name</b>
	    </div>
	    <div class="col-sm-9 form-group">
	      <%= @product.name.present? ? @product.name.capitalize : 'N-A' %>
	    </div>
	  </div>
   <div class="row">
	    <div class="col-sm-3 form-group">
	      <b> Description</b>
	    </div>
	    <div class="col-sm-9 form-group">
	      <%= @product.description.present? ? @product.description : 'N-A' %>
	    </div>
  </div>